import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const OnlineData = ({navigation}) => {
  return (
    <View>
      <Text>OnlineData</Text>
    </View>
  )
}

export default OnlineData

const styles = StyleSheet.create({})